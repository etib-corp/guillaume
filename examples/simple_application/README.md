# Simple application

This example demonstrates a basic application using the Guillaume framework. It sets up a simple application structure, initializes the framework, and creates a basic UI component.
