# Guillaume Examples

This directory contains example applications demonstrating the usage of the Guillaume C++ UI framework. Each example is built as part of the main Guillaume build process and can be found in the `build/examples` directory after building the project.

## Simple Application Example

The `simple_application` example showcases a basic application using the Guillaume framework. It demonstrates how to set up a simple application structure, initialize the framework, and create a basic UI component.
